# Always ignore this folder (even if it has .cpp/.h/.txt)
xhello_world/

# macOS noise
.DS_Store

# Allowlist approach: ignore everything by default…
*

# …but keep directories so Git can traverse
!*/

# …and keep only these file types anywhere
!**/*.cpp
!**/*.h
!**/*.txt

# Keep this file itself
!.gitignorexhello_world/
